# GSM Angular Frontend

* Install npm
* Install packages from **package.json** using **npm install**
* Install bower CLI
* Install packages from **bower.json** using **bower install**
* Update **js/config.js** to add your Django backend's base url
